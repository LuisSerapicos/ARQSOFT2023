package com.isep.acme.repositories.mongodb;
/*

import com.isep.acme.model.Product;
import com.isep.acme.repositories.DataBase;
import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Component;

import java.util.Optional;

    @Component("mongoDB")
        public interface MongoDbDataBase extends MongoRepository<Product, String>, DataBase {
        default Optional<Product> findBySku(String sku){
            System.out.println("Generate find MongoDB");
            return null;
        };
    }*/
